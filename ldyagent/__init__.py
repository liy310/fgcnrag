"""
林黛玉Agent模块
================

本模块是一个基于林黛玉角色的AI Agent系统，用于：
- 学生心理陪伴与情绪疏导
- 学业辅导（作文点评等）
- 诗词互动（飞花令、对联）
- 文学知识问答

系统架构：
┌─────────────────────────────────────────────────────────────────┐
│                         API接口层                                │
│  (api/academic.py, api/auth.py, api/chat_emotion.py,          │
│   api/poetry.py)                                               │
└─────────────────────────────────────────────────────────────────┘
                                ↓
┌─────────────────────────────────────────────────────────────────┐
│                       RAG问答链层                               │
│                  (chain/ldy_rag.py)                            │
│     • 调用DeepSeek LLM生成回复                                   │
│     • 构建包含角色设定、历史上下文、情绪信息的Prompt              │
│     • 管理会话状态和对话历史                                     │
└─────────────────────────────────────────────────────────────────┘
                                ↓
┌─────────────────────────────────────────────────────────────────┐
│                        服务层                                    │
│  (services/emotion_analyzer.py - 情绪分析                       │
│   services/couplet_service.py - 对联服务                        │
│   services/flying_flower_service.py - 飞花令服务                │
│   services/document_loader.py - 文档加载                        │
│   services/memory_compressor.py - 记忆压缩                      │
└─────────────────────────────────────────────────────────────────┘
                                ↓
┌─────────────────────────────────────────────────────────────────┐
│                       数据库层                                   │
│           (database/mysql_init.py, database/user_db.py)        │
│     • 会话管理、对话历史存储、情绪记录                            │
│     • 用户认证信息存储                                           │
└─────────────────────────────────────────────────────────────────┘
                                ↓
┌─────────────────────────────────────────────────────────────────┐
│                       人设层                                    │
│                   (persona/lin_persona.py)                     │
│     • 林黛玉角色设定                                             │
│     • 情绪关键词映射                                             │
│     • 诗词典故库                                                 │
└─────────────────────────────────────────────────────────────────┘

目录结构：
├── __init__.py          # 包初始化
├── auth.py               # JWT认证工具
├── config.py             # 配置管理
├── init_database.py      # 数据库初始化脚本
├── api/                  # API接口
│   ├── __init__.py
│   ├── academic.py       # 学业辅导（作文点评）
│   ├── auth.py           # 用户认证
│   ├── chat_emotion.py   # 对话与情绪疏导
│   └── poetry.py         # 诗词鉴赏、飞花令、对联
├── chain/                # RAG问答链
│   ├── __init__.py
│   └── ldy_rag.py        # 核心对话逻辑
├── database/             # 数据库操作
│   ├── __init__.py
│   ├── mysql_init.py     # MySQL初始化与操作
│   └── user_db.py        # 用户管理
├── persona/              # 角色设定
│   ├── __init__.py
│   └── lin_persona.py     # 林黛玉人设与诗词库
└── services/             # 业务服务
    ├── __init__.py
    ├── couplet_service.py         # 对联服务
    ├── document_loader.py         # 文档加载
    ├── emotion_analyzer.py        # 情绪分析
    ├── flying_flower_service.py    # 飞花令服务
    └── memory_compressor.py       # 记忆压缩
"""
