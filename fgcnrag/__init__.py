"""
FGCNRAG四大名著知识库系统
=========================

这是一个基于RAG（检索增强生成）的四大名著问答系统。

系统架构：
┌─────────────────────────────────────────────────────────────────┐
│                         用户请求层                               │
│              (FastAPI接口 /fgcn/chat)                           │
└─────────────────────────────────────────────────────────────────┘
                                ↓
┌─────────────────────────────────────────────────────────────────┐
│                      RAG问答链层                                 │
│  (qa_rag.py - 构建Prompt、调用LLM、答案校验)                     │
└─────────────────────────────────────────────────────────────────┘
                                ↓
┌─────────────────────────────────────────────────────────────────┐
│                       检索器层                                   │
│       (hybrid_retriever.py - 混合检索：向量+关键词)             │
└─────────────────────────────────────────────────────────────────┘
                                ↓
┌─────────────────────────────────────────────────────────────────┐
│                      向量数据库层                                 │
│           (Milvus - 存储和检索向量数据)                          │
└─────────────────────────────────────────────────────────────────┘

目录结构：
├── fgcn/                          # 核心模块包
│   ├── config.py                  # 配置管理
│   ├── insert_data.py              # 数据导入脚本
│   ├── api/                        # API接口
│   │   └── chat.py                 # 问答接口
│   ├── chain/                      # RAG问答链
│   │   └── qa_rag.py               # 核心问答逻辑
│   ├── chunker/                    # 文本分块
│   │   └── text_splitter.py        # 递归字符分块器
│   ├── database/                   # 数据库操作
│   │   └── vdb_init.py             # Milvus数据库初始化
│   ├── embedder/                   # 向量化
│   │   └── embedding.py            # 阿里百炼embedding
│   ├── loader/                     # 数据加载
│   │   ├── document_loader.py      # 通用文档加载
│   │   └── excel_loader.py         # Excel问答对加载
│   └── retriever/                  # 检索器
│       └── hybrid_retriever.py     # 混合检索器
├── data/                          # 原始数据目录
│   ├── *.txt                       # 四大名著原文
│   └── *.xlsx                      # 预处理问答对
├── check_vdb.py                    # 数据库状态检查脚本
└── delete_table.py                 # 删除数据库表脚本
"""
from .fgcn.config import settings

__all__ = ["settings"]
